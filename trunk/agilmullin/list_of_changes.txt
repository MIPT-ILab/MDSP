21:53 23.11.2010
WARNING! New core.cpp was tested only on Visual Studio 2008. (makefile isn't tested now. I'll do it soon.)
Столкнулся со странным багом GetPC() на четвёртой итерации возвращает неправильное значение PC. c вместо 12.

1) My name and surname in members.txt were interchanged.

2) Two strings in core.cpp were interchanged:
before

        /* Add 4 bytes (32 bits) to access next instruction */
        this->pc = this->pc + 4;

        /* Execute operation */
        op->execute();
        delete( op);

after

        /* Execute operation */
        op->execute();
        delete( op);

        /* Add 4 bytes (32 bits) to access next instruction */
        this->pc = this->pc + 4;

3) Two strings are added in operation.cpp after void Operation::execute():

	/* Print "execute" and current PC */
	cout << "execute PC=" << core->GetPC() << endl;

(Думаю коментарий здесь неуместен: и так всё понятно. Кроме того, по код стайлу здесь вместо core->GetPC() должно быть this core->GetPC(), но такой вариант у меня не коомпилится, постараюсь исправить.)

Arthur Gilmullin